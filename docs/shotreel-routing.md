# ShortReel routing

ShortReel is built with `basePath: "/shotreel"` and served by `blau-shortreel`
through `blau-app`'s `WEB_SHORTREEL` service binding. The prefix is preserved.

See [application routing](routing.md) for the complete route registry,
deployment order, account, connected preview setup, and verification commands.
