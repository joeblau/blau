<section class="section" id="features">
  <div class="container">
    <header class="section__head" data-reveal>
      <p class="eyebrow">The cockpit</p>
      <h2 class="h2">
        Everything an agentic workflow needs.<br />
        <span class="h2--muted">Nothing it doesn't.</span>
      </h2>
      <p class="lead">
        Pilot fuses the tools you alt-tab between into a single native surface,
        so you stay the reviewer while your agents do the typing.
      </p>
    </header>
    <div class="bento">
      <article class="card card--wide" data-reveal>
        <div class="card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <rect x="3" y="4" width="18" height="16" rx="3" />
            <path d="M7 9l3.5 3L7 15M12.5 15H17" />
          </svg>
        </div>
        <h3>Agent-grade terminals</h3>
        <p>
          GhosttyKit terminals render at full GPU speed, so coding agents stream
          diffs, tests, and logs without dropping a frame. Run several side by
          side and keep every one of them under your eye.
        </p>
      </article>
      <article class="card card--rest d-1" data-reveal>
        <div class="card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8l-5-5Z" />
            <path d="M14 3v5h5M9 13l2 2 4-4" />
          </svg>
        </div>
        <h3>Editing in context</h3>
        <p>
          A native editor lives beside every terminal. Review an agent's patch,
          refine it, and hand it back — without leaving the window.
        </p>
      </article>
      <article class="card" data-reveal>
        <div class="card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="12" cy="12" r="9" />
            <path d="M3.5 9h17M3.5 15h17M12 3a14.5 14.5 0 0 0 0 18M12 3a14.5 14.5 0 0 1 0 18" />
          </svg>
        </div>
        <h3>Live previews</h3>
        <p>
          Browser previews sit next to the code they render. Watch an agent's
          change rebuild and reload in place.
        </p>
      </article>
      <article class="card d-1" data-reveal>
        <div class="card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <circle cx="6" cy="6" r="2.4" />
            <circle cx="6" cy="18" r="2.4" />
            <circle cx="18" cy="8" r="2.4" />
            <path d="M6 8.4v7.2M18 10.4c0 4.5-4.5 4.6-8.5 4.9" />
          </svg>
        </div>
        <h3>GitHub, in flow</h3>
        <p>
          Issues, pull requests, and reviews are first-class. Triage and merge
          while your terminals keep working.
        </p>
      </article>
      <article class="card d-2" data-reveal>
        <div class="card__icon">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <rect x="7" y="2.5" width="10" height="19" rx="2.5" />
            <path d="M10.5 5h3" />
            <circle cx="12" cy="17.5" r="1.4" fill="currentColor" stroke="none" />
          </svg>
        </div>
        <h3>Simulators &amp; capture</h3>
        <p>
          Boot simulators, capture device screens, and record the result —
          deterministic demo state included.
        </p>
      </article>
    </div>
  </div>
</section>
